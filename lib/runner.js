const RaceTower = require('./RaceTower');

/**
 * Routes commands (strings) to their corresponding implementation and collects output
 * 
 * @param {string[]} input 
 * @return {string[]}
 */
function runner(input, raceTower) {
    raceTower = raceTower || new RaceTower();
    
    // @TODO ...
}

module.exports = runner;
